<nav class="navbar navbar-expand-lg navbar-dark sticky-top custom-navbar">
    <div class="container">
      <a class="p-0 m-0 logoandname d-flex justify-content-around align-items-center" href="/">
        <img class="navbar-brand navbar-brand me-2" src="./img/logo-square.png" alt="...">
        <p class="text-white d-inline m-0 fs-5 fw-medium">Global School Pune</p>
      </a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ms-auto">
        <% let individualNavLinks = navLinks.split(',') %>
            <% for (let i = 0; i < individualNavLinks.length; i++) { %>
                <% const [name, link] =  individualNavLinks[i].split('[') %>
                <li class="nav-item mx-3">
                    <a class="nav-link active mt-2" aria-current="page" href="<%= link.replace(']', '') %>"><%= name %></a>
                </li>
        <% } %>
        <li class="nav-item mx-3">
            <a class="nav-link last-nav-link mt-2 mb-2" href="#contact-info">CONTACT US</a>
        </li>
        </ul>
      </div>
    </div>
</nav>